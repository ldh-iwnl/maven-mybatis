# maven-mybatis <br />

demo2: use mybatis to do sql select with multiple parameters <br />
&emsp; 1. pass parameter map<br />
&emsp; 2. pass parameter directly using @Param to bind<br />
&emsp; 3. use pojo<br />
&emsp; 4. create dynamic sql select using <if> <where> tag in mybatis <br />
